const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { console.log(JSON.stringify(await prisma.financialTransaction.findMany(), null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
